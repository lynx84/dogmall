<%= form_for(crawling_product) do |f| %>
    <% if crawling_product.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(crawling_product.errors.count, "error") %> prohibited this crawling_product from being
            saved:</h2>

          <ul>
            <% crawling_product.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <div class="field">
      <%= f.label :product_url %>
      <%= f.text_field :product_url %>
    </div>

    <div class="field">
      <%= f.label :product_name %>
      <%= f.text_field :product_name %>
    </div>

    <div class="field">
      <%= f.label :image_url %>
      <%= f.text_field :image_url %>
    </div>

    <div class="field">
      <%= f.label :cost %>
      <%= f.text_field :cost %>
    </div>

    <div class="field">
      <%= f.label :product_info %>
      <%= f.text_field :product_info %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
